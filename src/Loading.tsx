import React from 'react'

const Loading: React.FC = () => (
    <div style={{
        fontFamily: 'sans-serif',
        marginLeft: '20px',
        height: '100vh',
        display: 'flex',
        alignItems: 'center'
    }}>
        loading
    </div>
)

export default Loading