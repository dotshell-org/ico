export enum SummaryProperty {
    Date = "date",
    Title = "title",
    Amount = "amount",
    Category = "category"
}

export enum SummaryPropertyIndex {
    Date = 0,
    Title = 1,
    Amount = 2,
    Category = 3
}